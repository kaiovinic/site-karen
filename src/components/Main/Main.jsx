import styles from "./Main.module.css";

const Main = () => {
  return (
    <main>
      <section className={`${styles.paralax01} ${styles.paralax}`}></section>
    </main>
  );
};

export default Main;
